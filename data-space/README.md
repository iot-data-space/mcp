# Testing data space
A data space for testing mcp functionality

## Execution

```
docker compose -f docker-compose/compose.yml  up
```

The first time this script is execute may result in error. Stop all containers and restart

## Data space initialization 

Execute the python script `initialize_broker.py`
